__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.DS_Store

# Data and run outputs are never committed. Anchored to the repo root on
# purpose: an unanchored `runs/` also matches src/masattr/runs/, which is a
# package, not an output directory.
/data/
/runs/
/out/
*.pkl

# Fitted normalization statistics are a run artifact — they depend on which
# judge scored the corpus — so they live under /runs/ and are not tracked.
