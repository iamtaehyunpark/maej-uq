# Archived reports

Superseded by [`../RESULTS.md`](../RESULTS.md), which consolidates every result
into one generated document. These are kept for history only.

| file | what it was | why it is here |
|---|---|---|
| `pilot_baseline_report.md` | the baseline suite write-up | folded into `RESULTS.md` |
| `delta_field_report.md` | the lookahead-shift experiment | folded into `RESULTS.md` |
| `E1_report.md` | the reference row in full detail | folded into `RESULTS.md`; its rule sweep is now a section there |

**Do not cite numbers from these files.** `E1_report.md` in particular went
through two states: the original hand-written version used a prompt scaffold
that was later corrected, and every judged figure in it was wrong by the time
it was replaced. It was regenerated once before being archived, but
`RESULTS.md` is the only file that is rebuilt from current run outputs.
